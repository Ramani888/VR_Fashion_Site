import React from 'react'

const useRegister = () => {
    const handleRegister = () => {
        try {

        } catch (err) {
            console.log(err);
        }
    }
}

export default useRegister